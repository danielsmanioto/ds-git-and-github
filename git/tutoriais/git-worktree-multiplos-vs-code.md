# Tutorial: Git Worktree com múltiplos projetos no VS Code

## Vá até source > preferences > settings 

Procure por 
Se no seu VS Code essa opção estiver como `single`, sem problema.

Para usar `git worktree`, o que realmente importa é:

- abrir cada worktree como pasta separada, ou
- adicionar várias worktrees ao mesmo workspace
- scm source controle 
- sete a opcao single
- ative a opcao repositorio em source controle
- pronto